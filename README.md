# v0.8.33

This release refines COM/USB boot-picture upload session entry.

The boot-picture uploader now enters the initial PROGRAM/F/M identification phase and sends the boot-picture D command before the normal SEND challenge. Normal radio read/write behavior is unchanged.
